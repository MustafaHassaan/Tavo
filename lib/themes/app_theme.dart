import 'package:flutter/material.dart';

class AppTheme {
  static final ThemeData lightTheme = ThemeData(
    primarySwatch: Colors.yellow,
    scaffoldBackgroundColor: Colors.transparent,
    fontFamily: 'Roboto', // أو أي خط تحب تستخدمه
  );
}
